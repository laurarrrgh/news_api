const { fetchArticle } = require("../models/articleModel.js");

exports.getArticle = (req, res, next) => {
  // const { username } = req.params;
  // fetchArticle(username)
  //   .then(user => {
  //     res.status(200).send({ user });
  //   })
  // .catch(next);
};
